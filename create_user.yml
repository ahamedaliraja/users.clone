---
- name: user add
  hosts: prod
  vars_files:
    user_list.yml
  tasks:
    - name: User add task
      vars:
        password: "{{ lookup('password','filnena-'+item.name+' length=6') }}"
      ansible.builtin.user:
        name: "{{ item.name }}"
        uid: "{{ item.uid }}"
        password: "{{ password|password_hash('sha512') }}"
        state: present
        comment: "{{ item.first|capitalize }} {{ item.middle|capitalize }} {{ item.last|capitalize }}"
      loop: "{{ users }}"
